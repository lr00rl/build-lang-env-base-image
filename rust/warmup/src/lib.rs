//! Empty crate whose dependencies are the data-query stack. rust-base
//! `cargo fetch`es and, when PRECOMPILE cooks this arch, `cargo build`s it
//! so service images reuse rlibs under CARGO_TARGET_DIR=/opt/rust-cache.
