# The image FROMs official rust / debian, apt-get's the toolchain, rustup
# target add's both GNU triples, and cargo fetch's warmup/. Nothing else
# from this directory belongs in the context.
*
!cargo-config.toml
!warmup/
!warmup/**
warmup/target
warmup/**/target
